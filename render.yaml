services:
  - type: web
    name: angular-ssr
    runtime: node
    plan: free
    buildCommand: npm install && npm run build:ssr
    startCommand: node dist/utr-predictor/server/server.mjs
    envVars:
      - key: NODE_ENV
        value: production
    autoDeploy: true
